#include<stdio.h>
int main()
{
	int n1,n2,i,odd;
	printf("Enter the value of n1:");
	scanf("%d",&n1);
	printf("Enter the value of n2:");
	scanf("%d",&n2);
	for(i=1;i<=n2;i++)
	{
		odd=odd+2;
		if(odd<n2 && i++!=5)
		printf("%d\n",odd);
		
	}
}
